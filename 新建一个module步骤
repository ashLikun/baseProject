1:复制(module_备份)，改包名。或者直接新建(包名复制app里面的temp)，gradle要写好，参照其他module
2:配置好gradle   1:新建的模块gradle第一行module名称     2：app gradle里面加上依赖    3:settings.gradle加上本模块
3:填写好说明文档
4：在app的application(MyApp)添加...module名字/ModuleApp。
5:添加javabean混淆（非必要）